#include <iostream>
#include "Models.hpp"
#include "MainDb.hpp"

int main() {
    // MainDb db;
    // db.connectToDb("127.0.0.1");
    // User newUser("hui", "hui4", {"1", "huyy", "3"}, "online", 1);
    // db.writeUser(newUser);
    // User* result = db.searchUserLogin(newUser.getLogin(), newUser.getPassword());
    // std::cout << result->getLogin() << " " << result->getPassword() << std::endl;
    // for (int i = 0; i < result->getDialogues().size(); i++) {
    //     std::cout << result->getDialogues()[i] << " ";
    // }
    // std::cout << std::endl;
    // db.updateUser(newUser);
    // db.disconnectFromDb();
    return 0;
}
